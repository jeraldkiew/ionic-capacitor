import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tradesocio.wt5',
  appName: 'Hello-world',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
